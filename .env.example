# SecondBrain environment configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Path to your Markdown notes folder (required)
# Works with Obsidian vaults, plain .md folders, or any Markdown-based note system
SECONDBRAIN_VAULT_PATH=/path/to/your/notes

# API server settings
SECONDBRAIN_HOST=127.0.0.1
SECONDBRAIN_PORT=8000

# Data storage directory (relative to project root)
SECONDBRAIN_DATA_PATH=data

# --- LLM Providers ---
# At least one is recommended. For fully local operation, use Ollama (no keys needed).

# Anthropic Claude (preferred for chat quality)
SECONDBRAIN_ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (alternative API provider)
SECONDBRAIN_OPENAI_API_KEY=sk-...

# Local LLM via Ollama (no API key needed — install Ollama and pull a model)
# See: https://ollama.com
SECONDBRAIN_OLLAMA_MODEL=llama3.2:3b
SECONDBRAIN_OLLAMA_BASE_URL=http://127.0.0.1:11434/v1

# --- Embeddings ---
# "local" downloads BAAI/bge-base-en-v1.5 (~400MB) on first run — no API key needed
# "openai" uses text-embedding-3-small (requires SECONDBRAIN_OPENAI_API_KEY)
SECONDBRAIN_EMBEDDING_PROVIDER=local
SECONDBRAIN_EMBEDDING_MODEL=BAAI/bge-base-en-v1.5
SECONDBRAIN_OPENAI_EMBEDDING_MODEL=text-embedding-3-small
